# Copy to ../.env, keep mode 0600, and never commit the result.
MEMBERBERRY_ADMIN=alice
MINIO_ROOT_USER=memberberry
# Generate with: openssl rand -hex 32
MINIO_ROOT_PASSWORD=replace_with_a_random_secret
MINIO_BUCKET=memberberry
MEMBERBERRY_PORT=9010
MEMBERBERRY_BACKUP_DIR=./backups
